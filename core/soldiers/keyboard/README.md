# core/soldiers/keyboard

Keyboard layouts on the Steam Deck.

- `keyboard_layout_soldier.py` — turns the PC's languages into XKB layouts and writes them for game mode (`~/.config/environment.d`) and the KDE desktop (`~/.config/kxkbrc`), with Alt+Shift to switch.
