# core/officers/intelligence

Read-only observers: they change nothing and only answer questions about the Deck.

- `deck_session_sensor.py` — screen size, whether the screen is on, Game Mode or desktop, whether a game has captured the cursor; `/sys` and `/proc` roots are parameters so tests can use fake trees.
