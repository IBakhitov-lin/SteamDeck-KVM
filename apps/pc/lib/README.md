# apps/pc/lib

Tray helpers bundled with the Windows app.

- `tray-common.ps1` — single instance, tray icon, color from the palette.
- `tray-place.ps1` — where the tray popup appears and how it is laid out.

Generated by `private_tools/scripts/build_release_script.py`; edit the source, not the copies.
