# Python
__pycache__/
*.pyc
.pytest_cache/

# Сборка выпуска: архивы собираются private_tools/scripts/build_release_script.py и
# публикуются выпуском GitHub, в истории репозитория им не место.
dist/

# Собираемое: ярлык несёт абсолютные пути одной машины, значки рисует оболочка окна.
# Первоисточник — private_tools/scripts/make_shortcuts_script.ps1 и сборщик выпуска.
*.lnk
*.ico
apps/deck/steamdeck-kvm.png

# Корзина проекта: перенесённое сюда не удалено, но и не часть продукта.
.trash/

# Инструкции ИИ домена. Репозиторий публичный, а инструкции несут пути, журналы и решения
# автора: история у них своя, во вложенном приватном репозитории .agents/.
# Правило — C:\AI\github_canon.md, «Что кладётся в публичный репозиторий».
.agents/
AGENTS.local.md
CLAUDE.local.md
