# Python
__pycache__/
*.pyc
.pytest_cache/

# Сборка выпуска: архивы собираются private_tools/scripts/build_release_script.py и
# публикуются выпуском GitHub, в истории репозитория им не место.
dist/

# Собираемое: ярлык несёт абсолютные пути одной машины, значок Windows рисует оболочка окна.
# Значок Steam Deck хранится в истории: Deck ставится из архива исходного кода выпуска.
# Первоисточник — private_tools/scripts/make_shortcuts_script.ps1 и сборщик выпуска.
*.lnk
*.ico

# Корзина проекта: перенесённое сюда не удалено, но и не часть продукта.
.trash/

# Локальные заметки сопровождающего для ИИ-ассистентов — в репозиторий не входят.
.agents/
AGENTS.local.md
CLAUDE.local.md
